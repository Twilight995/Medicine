# 약찾아삼만리 — 구현 명세

목업: `Mock/Medicine.pen` (행 1~8이 확정본. y=11,200 아래는 시안·비교용이므로 구현 대상이 아님)
기준 문서: `약찾아삼만리_02_목업_화면명세서.md`
작성: 2026-09-20 (검토 반영 1차)

---

## 1. 무엇을 만드는가

처방약·일반약의 재고를 주변 약국 여러 곳에 한 번에 묻고, 답을 있는 그대로 보여 주는 앱. 사용자 앱과 약국 앱이 한 앱 안에 있고 첫 실행에서 역할을 고른다.

**앱이 하지 않는 것** (설계 제약이므로 구현에서도 지킨다)
- 가격·할인·결제·배송·예약금·별점·추천·인기순 기능
- 약국 간 우열을 암시하는 표시 (강조색 테두리, 큰 카드, 배지, 응답 속도)
- 증상으로 약 찾기
- 약국별 예상 응답 시간

---

## 2. 화면 목록 (33개 + 상태 변형 67개 = 100장)

| 흐름 | 화면 수 | 화면 |
|---|---|---|
| 사용자 시작 | 8 | U-01 역할 선택 / U-02 온보딩 3장 / U-03 필수 동의 / U-04 홈 (+진행 중 문의 없음, 방문 예정 있음) |
| 처방약 입력 | 12 | U-05 촬영 가이드 / U-06 촬영 (+마스킹 확인, 읽는 중, 읽기 실패) / U-07 읽은 결과 확인 (+전체·일부·선택 없음) / U-08 약 직접 찾기 (+입력 전, 결과 없음) |
| 일반약 입력 | 4 | U-09 일반약 검색 (+입력 전, 전문의약품, 결과 없음) |
| 문의 흐름 | 31 | U-10 약품 정보 (4) / U-11 위치 권한 (2) / U-12 반경 확인 (3) / U-13 문의 대기 (2) / U-14 응답 목록 (10) / U-15 약국 상세 (3) / U-16 전화 재확인 (2) / U-17 방문 예정 보내기 (2) / U-18 방문 진행 (3) |
| 사용자 부가 | 7 | U-19 복용 중 약 / U-20 문의 기록 / U-21 신고·정정 / U-22 설정 |
| 약국 시작 | 5 | P-01 약사 가입 (4) / P-02 수신 동의 |
| 약국 응답 | 26 | P-03 문의 수신함 (6) / P-04 문의 상세 (5: 기본·a 만료·b 이미 해결·c 처방약 한 개·d 일반약) / P-05 후보 선택 (7) / P-06 응답 완료 (2) / P-07 방문 예정 (6) |
| 약국 관리 | 7 | P-08 자동 응답 품목 관리 (3) / P-09 자동 응답 재확인 / P-10 설정 (2) / P-11 응답 통계(자리만) |

화면 코드는 목업의 프레임 이름과 1:1로 맞춘다. 상태 변형은 기본 화면 코드 + 소문자(U-14b, P-04d …).

---

## 3. 디자인 토큰

목업의 모든 값이 아래 토큰을 참조한다. 구현에서도 하드코딩하지 않는다.

### 색 (26)
| 역할 | 토큰 | 값 |
|---|---|---|
| 바탕·주 버튼 | `ink` | #061426 |
| 어두운 면 위 한 단 올라온 박스 | `midnight` | #12263F |
| 답이 왔다·진행 (어두운 바탕 전용) | `mint` | #32E3B1 |
| 로고 그라데이션 전용 (UI 금지) | `teal` | #20CFA6 |
| 일반약 계열 | `sky` | #62D3FF |
| 밝은 면의 긍정 상태 글자 | `teal-text` | #0A7D66 |
| 연한 바탕 | `mint-soft` #E3FBF4 / `sky-soft` #E6F7FF / `warn-soft` #FFF4E0 | |
| 일반약 진한 글자 | `info-text` | #0B5E85 |
| 주의·만료·확인 필요 | `warn` | #B45309 |
| 어두운 바탕 위 주의 | `warn-on-dark` | #FFB454 |
| 삭제·오류 | `danger` | #C2334D |
| 화면 배경 / 카드 / 처방전 종이 | `bg` #F2F6FB / `surface` #FFFFFF / `paper` #FBFCFE |
| 글자 | `text` #061426 / `text-2` #4A5A70 / `text-3` #5F6E83 |
| 어두운 바탕 글자 | `on-dark` #FFFFFF / `on-dark-2` #A9B9CF |
| 선 | `line` #DFE6EF (구분선) / `line-strong` #7B889A (누르는 것의 테두리) |
| 어두운 바탕 선 | `on-dark-line` #FFFFFF2E / `on-dark-stroke` #FFFFFF3D |

모든 글자·배경 조합은 WCAG AA를 통과한다(본문 4.5:1, 큰 글자 3:1).

### 글자
서체 하나: **IBM Plex Sans KR** (`font-body`). 숫자도 같은 서체.

| 토큰 | px | 용도 |
|---|---|---|
| `fs-display` | 56 | 거리, 남은 시간 |
| `fs-hero` | 36 | 홈·온보딩 제목 |
| `fs-h1` | 28 | 화면 제목 |
| `fs-title` | 20 | 강조 문장, 항목 이름 |
| `fs-body` | 16 | 본문, 버튼 |
| `fs-sub` | 14 | 보조 설명, 고지문 |
| `fs-cap` | 12 | 칩, 캡션 (최소) |

굵기 `fw-regular` 400 / `fw-medium` 500 / `fw-bold` 700
줄 높이 `lh-title` 1.3 / `lh-body` 1.5 / `lh-number` 1.0
자간 `ls-title` -0.5 (20~28px) / `ls-display` -1.5 (36px 이상) / 본문 0

### 모서리 — 위계를 나타낸다
`r-xl` 32 화면을 이루는 면 / `r-lg` 20 누르는 것 / `r-md` 12 면 안에 담긴 것 / `r-sm` 8 이름표 / `r-full` 진행선·원

### 간격
`sp-half` 2 · `sp-1` 4 · `sp-2` 8 · `sp-3` 12 · `sp-4` 16 · `sp-5` 20 · `sp-6` 24 · `sp-8` 32 · `sp-10` 40 · `sp-12` 48

### 그 밖
아이콘 `icon-sm` 16 / `icon-md` 20 / `icon-lg` 24
선 두께 `stroke-hair` 1 / `stroke-ctrl` 1.5 / `stroke-mark` 3
최소 터치 영역 `hit-min` 44

### 아이콘
- 내용 아이콘: **Phosphor 채움**(`-fill`). 연한 타일 위에 같은 계열 진한 색
- 선형 유지(lucide): 상태바, 뒤로가기, 쉐브론, 닫기
- 타일: 큰 것 52·r-md, 작은 것 36·r-sm

---

## 4. 컴포넌트 (행 1~8이 쓰는 34개)

버튼 Primary / Secondary / Accent / Disabled / Link / OnDark
칩 Neutral / Mint / Warn / Dark / Sky / OnDark
안내 Note Box (Info·Warn·Safe) / Note Fine (Teal·Warn — 왼쪽 3px 막대)
목록 Row/Med · Row/Setting · Divider Line · Divider Dash · Segment On/Off
카드 Response Card (거리 + 이름 + 상태 + 액션) / Action Default·Primary
공통 Status Bar · Top Nav · Title Block · Big Number · Lane/6 · Icon Tile · Tab Bar

`v3/…` 접두사가 붙은 28개는 폐기된 시안용이므로 구현 대상이 아니다.

---

## 5. 응답 카드 표시 규칙 (가장 중요)

응답 카드는 이 앱의 핵심이고, 법적 방어가 걸린 부분이다.

### 구성
| 줄 | 내용 | 조건 |
|---|---|---|
| 1 | 거리 (`fs-title`, 카드 왼쪽) | 항상 |
| 2 | 약국 이름 + `시연용` 칩 + 자동 응답 배지(네이비 바탕 흰 글씨) | 배지는 자동 응답일 때만 |
| 3 | 상태 줄 `응답 시점` │ `재고 상태` — 두 부분을 **연한 세로선**으로 나눈다 | 항상 |
| 4 | 유효시간 `N분간 유효` (작은 회색) | **직접 응답 + 아직 유효할 때만** |
| 5 | 주의 문구 (주황 글씨, 배경·아이콘 없음) | 해당 상태만 |
| 6 | 등록 운영시간 + 영업 여부 | 항상 |
| 7 | 액션 버튼 | 상태에 따라 2~3개 |

### 상태별 표시
| 상태 | 3줄 | 4줄 유효시간 | 5줄 주의 문구 | 7줄 버튼 |
|---|---|---|---|---|
| 직접 응답 · 유효 | `3분 전 응답 │ 전부 있음` (재고 상태만 teal-text) | **표시** `N분간 유효` | 없음 | 전화 · 길찾기 · 방문 예정 |
| 직접 응답 · 후보 상담 | `1분 전 응답 │ 후보 상담 가능` (재고 상태만 teal-text) | **표시** | `약사와 상담 필요` (+ 약국이 골랐다면 `후보 상담: △△정`) | 전화 · 길찾기 · 방문 예정 |
| 자동 응답 | `2일 전 등록 │ 전부 있음` (회색) | **표시 안 함** | `방문 전 확인 권장` | **전화(주 버튼)** · 길찾기 · 방문 예정 |
| 만료 | `12분 전 응답 │ 재확인 필요` (회색) | **표시 안 함** | `응답이 오래되었습니다. 전화로 확인하세요.` | 전화 · 길찾기 (**방문 예정 없음**) |

- 유효시간의 `7분`은 목업의 예시일 뿐이다. 실제로는 `응답 시각 + 유효기간(기본 10분) − 현재 시각`을 분 단위로 계산해 표시한다. 0이 되면 만료 상태로 바꾼다.
- 자동 응답의 기준 시각은 **품목을 켜거나 마지막으로 갱신한 때**이지 문의가 도착한 때가 아니다.
- 경과 시간은 절대시각("14:32")이 아니라 "○분 전".

### 카드 규칙
- 모든 카드는 **같은 폭·같은 기본 스타일**을 쓴다. 내용에 따라 높이는 달라진다.
- **우열을 암시하는 표시 금지**: 추천·인기·재고확실 배지, 강조색 테두리, 다른 카드보다 큰 크기, 응답 속도 표시, 광고 표시. **상태를 알리는 배지(자동 응답)는 허용**한다.
- 거리순 고정. 정렬 변경·필터 UI 없음. 기본 선택된 카드 없음.
- 새 응답은 맨 위가 아니라 거리 순서 자리에 들어간다. 화면 밖이면 "새 응답 N건" 알약 버튼.
- 만료된 카드는 지우지 않고 `재확인 필요`로 바꿔 제자리에 둔다.
- 상태 줄의 세로선은 `line` 굵기 1, 높이는 글자 높이만큼. 구분 기호(`·`)를 글자로 찍지 말고 **선 요소**로 그린다. 글자 크기가 커져도 두 부분이 붙어 보이지 않게 좌우 `sp-2` 간격을 둔다.
- 주의 문구는 상태 줄에 이어 붙이지 않고 **항상 다음 줄**에 주황 글씨로 쓴다. 배경 상자·아이콘 없음.

## 6. 반응형

- 폭 360~430 기본. 480 초과 시 내용 폭 480 제한 + 가운데 정렬
- 화면 높이 고정 금지. 내용은 스크롤, 하단 주 버튼은 떠 있는 층 (U-05 방식: 내용이 버튼 뒤로 흐리게 스며듦)
- 긴 문장은 전부 줄바꿈. **말줄임(…) 금지** — 법적 고지가 잘리면 안 된다
- 터치 영역 최소 44. 카드 액션 버튼은 **보이는 높이 36 + 상하 패딩 4**로 44를 만든다.
  단순히 바깥에 여백을 두면 안 되고, 패딩까지 실제로 눌려야 한다. Flutter에서는
  `InkWell`(또는 `GestureDetector`)을 패딩 **바깥**에 두고, `MaterialTapTargetSize.padded`
  또는 `ConstrainedBox(minHeight: 44)`로 히트 영역을 보장한다.
- 하단 주 버튼 56
- 시스템 글꼴 1.3배까지 견딜 것. 버튼 높이는 고정이 아니라 최소값
- 검증: 목업 맨 아래 `해상도 점검 · 360/390/430` 행

---

## 7. 문구 규칙

- 부록 B의 고정 문구 16종은 **글자 하나도 바꾸지 않는다**
- 금지 표현: 대체 가능 약 / 재고 보장·확실 / 실시간 운영시간 / OCR로 인식한 약 / 완치·무조건 안전·부작용 없음
  - 각각 이렇게 쓴다: 동일 조건 참고 후보 / (보장 표현 없음) / 등록 운영시간 / 확인 완료된 약
- 상태를 색만으로 구분하지 않는다. 항상 글자가 같이 간다
- 가상 약국·약 이름에는 `시연용` 표시
- 용어: 콜 → **문의**, 토글 → **자동 응답**

---

## 8. 명세에서 벗어난 것

### 승인된 것 (표시 방법의 변경)
1. 자동 응답 카드 본문에서 `(자동 응답)` 문구를 빼고 **배지**로 대체
2. 유효시간을 상태 줄에서 분리해 작은 회색 줄로 내림 (명세의 "줄바꿈 허용" 범위)

### 승인된 것 (이어서)
3. **집계 줄의 이름과 모양을 바꿨다.** 기획서의 `가능 N곳`을 `목록에 표시` 칸으로 쓰고, 한 문장으로 잇던 집계를 설명·숫자 칸으로 나눴다. 숫자의 정의는 기획서 그대로이고 이름과 배치만 바꿨다.
   - 이유: 목록에는 만료돼 `재확인 필요`가 된 카드도 남아 있고, `일부만 있음`으로 답한 약국도 들어 있다. 이것을 "가능"이라고 부르면 재고나 조제 가능성을 보장하는 것처럼 읽힌다 (11장·17-3의 보장 표현 금지와 충돌).

### 집계 줄 (확정)

집계는 한 문장으로 잇지 않고 **칸으로 나눈다.** 칸마다 위에 작은 회색 설명(`fs-cap`), 아래에 숫자(`fs-title`)를 놓고 가로로 나란히 둔다.

| 칸 | 설명(윗줄) | 숫자(아랫줄) | 뜻 | 포함 | 제외 |
|---|---|---|---|---|---|
| 1 | 문의한 약국 | `5곳` | 문의를 보낸 약국 수 | 전부 | — |
| 2 | 응답한 약국 | `5곳` | 최종 응답을 보낸 약국 수 | 재고 없음으로 답한 곳 포함 | 무응답, 확인 전, `확인 필요`만 누른 곳 |
| 3 | 목록에 표시 | `4곳` | 지금 목록에 카드로 보이는 수 | 만료(재확인 필요), 자동 응답, 일부만 있음 포함 | 재고 없음 |
| 4 | 확인 중 | `1곳` | `확인 필요`로 답하고 최종 응답을 기다리는 수 | — | — |

- 기획서의 `가능 N곳`에 해당하는 칸이 **목록에 표시**다(위 3번 참고).
- 3번 칸의 숫자와 실제 카드 수는 **항상 일치해야 한다.** 화면을 만들 때 하드코딩하지 말고 카드 목록 길이에서 계산한다.
- 응답이 0건이면 **1·2번 칸만** 보여 준다. 목록 칸은 아예 그리지 않는다.
- 4번 칸은 확인 중인 곳이 있을 때만 그린다.
- 집계 줄에 경고성 문구(예: `모두 재확인 필요`)를 섞지 않는다. 경고는 카드 안의 주황 줄이 맡는다.

## 9. 화면 동작 규칙 (상태 → 조건 → 결과)

| # | 언제 | 조건 | 결과 |
|---|---|---|---|
| 1 | 약국이 `확인 필요`를 누름 | — | 사용자 목록에 **카드로 나오지 않는다.** 집계의 "확인 중 N곳"에만 반영. 약국 쪽은 "최종 응답 대기"로 남고 P-06a에서 최종 응답 |
| 2 | 약국이 `없음`(전부 없음)으로 답함 | — | 카드 없음. "응답한 약국"에는 포함, "목록에 표시"에서는 제외 |
| 3 | 약국이 `일부만 있음`을 고름 | **없는 약을 1개 이상 선택해야** 전송 가능 | 미선택이면 전송 버튼 비활성 (P-05e) |
| 4 | 약국이 `후보 상담 가능`을 고름 | 후보를 고르지 않아도 전송 가능 | "고르지 않고 바로 보내기" 허용. 후보는 생동성 인정품목 목록에서만 선택, 자유 입력 금지 |
| 5 | 응답 후 유효기간(기본 10분) 경과 | — | 카드가 `재확인 필요`로 바뀌고 방문 예정 버튼이 사라짐. 약국 쪽 문의는 "만료" |
| 6 | 사용자가 방문 예정을 보냄 | — | 선택한 약국은 P-07, **나머지 약국에는 "해결됨"으로만 전달**(어느 약국을 골랐는지는 알리지 않음) |
| 7 | 방문 예정 보내기 화면의 연락처 동의 | **체크했을 때만** | 선택한 그 약국 1곳에만 휴대전화 번호 전달. 미체크여도 방문 예정은 보낼 수 있다. 방문 예정 종료 시 삭제 |
| 8 | 자동 응답 품목이 등록된 상태에서 문의 도착 | 문의에 담긴 약이 **전부** 등록돼 있을 때만 | 자동 응답. 하나라도 빠지면 사람에게 간다 |
| 9 | 자동 응답 등록 후 7일 경과 | — | 자동으로 꺼지고 재확인 알림 (P-09) |
| 10 | 위치 권한 거부 | — | U-11a. 주소 직접 설정 또는 설정 열기 |
| 11 | 반경 안에 참여 약국 0곳 | 최대 반경(3km) 전 | 반경 넓히기 버튼 (U-12a) |
| 12 | 최대 반경에서도 0곳 | — | 넓히기 버튼 없음, "이 범위에서는 응답한 약국이 없습니다." (U-12b·U-14d) |
| 13 | 일반약 검색에서 전문의약품을 찾음 | — | "처방전이 필요한 약입니다." + 처방전 촬영으로 유도 (U-09c) |
| 14 | 평균 응답 시간 표본 10건 미만 | — | 그 줄을 아예 표시하지 않는다 (U-13b) |
| 15 | OCR 매핑 실패 행 | 수정 전 | 그 행은 체크 불가. 체크 0개면 다음 버튼 비활성 (U-07c) |

세부 근거는 화면 명세서의 각 화면 절(U-07·U-13·U-14·U-17·P-04·P-05·P-08)에 있다.

## 10. 구현 순서 제안

1. 토큰과 컴포넌트를 먼저 코드로 옮긴다 (테마 + 공용 위젯)
2. 문의 왕복 경로: U-04 → U-10 → U-12 → U-13 → U-14 → U-15 → U-16 → U-17 → U-18 와 P-03 → P-04 → P-06 → P-07
3. 입력 경로: U-05 → U-06 → U-07 (OCR), U-09 (검색)
4. 약국 관리: P-08 → P-09
5. 시작·부가 화면
6. 상태 변형 67장은 각 화면의 분기로 구현

OCR은 기기 안에서 처리하고 사진을 저장하지 않는다. 마스킹은 촬영 직후 적용한다.
